import { useLocation } from 'react-router-dom';
import s from './MovieList.module.css';

const CamperCard = ({ movie }) => {
  const location = useLocation();
  return (
    <ul className={s.list}>
          
    </ul>
  );
};

export default CamperCard;